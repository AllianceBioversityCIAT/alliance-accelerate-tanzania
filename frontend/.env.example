# Base URL for the NestJS REST API (API Gateway endpoint).
# In production this is the CloudFront /api prefix or the API Gateway stage URL.
# Example: https://abc123.execute-api.eu-west-1.amazonaws.com/dev
NEXT_PUBLIC_API_BASE_URL=
