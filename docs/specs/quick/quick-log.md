# Quick Changes Log

One-line record of trivial, fast-tracked changes made with `/akili-quick`.

| Date | Change | Files | Verification | Commit |
|---|---|---|---|---|
| 2026-08-06 | quick/market-location-hint — add a hint to the public registration form's undocumented `marketLocation` field | frontend/components/register/RegistrationForm.tsx | `npm test` 1151 pass · `npm run lint` clean (no new warnings) | [SPEC:quick/market-location-hint] |
| 2026-09-10 | quick/metrics-error-state — ATP-50: surface `error` from `useMetrics` and state the failure once below the metrics grid (em-dashes stay, FR-3 intact). ⚠️ Exceeded the cosmetic gate — adds a derived flag + a render branch, and took new tests — fast-tracked with the user's explicit go-ahead; scope stayed frontend-only (no data/API/auth/PII) and mirrors the existing `useActors` convention. | frontend/lib/api/useMetrics.ts · frontend/components/home/MetricsBand.tsx (+ both test files) | `npm test` 1684 pass (111 suites) · `npx tsc --noEmit` clean · `npm run lint` no new warnings · `npm run build` static export OK · CDP capture at 375/768/1440 with the API unreachable: notice present, `scrollWidth == clientWidth`, page `docOverflow: 0` | [SPEC:quick/metrics-error-state] |
| 2026-09-10 | quick/metrics-error-state (2/2) — ATP-50 follow-up: the same silent failure in `CropCoverage`'s three crop cards, plus a correction to 1/2's dimming mechanism. **Finding, NOT fixed here:** no `/NN` opacity modifier on a semantic token survives the build — `tailwind.config.ts` defines every colour as `var(--color-x)`, which Tailwind cannot compose an alpha into, so `text-bg/80`, `divide-bg/20`, `bg-warning/10` and `border-danger/30` all render at FULL opacity. Zero such classes exist in the built CSS. `frontend/CLAUDE.md` currently calls them "acceptable precedent"; they are inert. | frontend/components/home/CropCoverage.tsx · frontend/components/home/MetricsBand.tsx (+ both test files) | `npm test` 1687 pass (111 suites) · `npx tsc --noEmit` clean · `npm run lint` 0 errors · `npm run build` OK · CDP at 375/768/1440 with the API down: notice present, grid still 3 children (not a 4th column), 24px gap, card opacity 1 after scroll, `docOverflow: 0`; contrast measured 9.55:1 (band) and 5.56:1 (crop) | [SPEC:quick/metrics-error-state] |
| 2026-09-10 | quick/admin-warning-affordances — three admin dialog backdrops used the inert `bg-fg/40` and opened with **no dimming at all**; swapped to the canonical `bg-backdrop` token, which `ConfirmDialog`/`AcknowledgeDialog`/`RejectDialog` already used. `globals.css:25` had documented the reason since the token was created ("the hex fg var doesn't support Tailwind /opacity modifiers") — the knowledge existed in the repo and never reached the guide. The 8 warning-chip fills/borders in the same request were NOT done — correctly deferred, but ⚠️ **the reason recorded here at the time was wrong**: it claimed a new `--color-warning-soft` token was needed, escalating to `/akili-propose`. It is not. `bg-surface-alt text-warning` is the established pattern — `lib/content/registration-status.ts:43` uses it for the Pending-review chip, which is why that chip shows its fill and the `bg-warning/10` ones do not. So the work is a `/akili-quick`, not a spec cycle, if it is ever wanted. It stays undone on merit, not on cost: the callouts read correctly without the wash (each carries explicit text plus `text-warning` colour coding), and on `DuplicateWarningCard` an amber parent would stack a third background behind two nested white candidate cards. Reviewed against the live screen 2026-09-10. | frontend/app/(admin)/admin/users/page.tsx · frontend/components/admin/CreateUserDialog.tsx · frontend/components/admin/EditUserDialog.tsx | `npm test` 1688 pass (111 suites, re-run after merging `main`) · `npx tsc --noEmit` clean · `npm run lint` 0 errors · `npm run build` OK · `.bg-backdrop` present in the built CSS and no `bg-fg/40` rule anywhere in it · rendered before/after in a scratchpad harness against the built stylesheet (no Cognito needed): `bg-fg/40` shows zero dimming, `bg-backdrop` shows the intended 40% wash | [SPEC:quick/admin-warning-affordances] |
| 2026-09-21 | quick/atp-68-scope-decisions — **scope cancellations, documentation only.** ATP-53 (server-side role-aware admin export) cancelled and closed: removed from PRD §5 In Scope, entered in Out of Scope, dropped from the Admin persona row, its `GET /api/v1/export` row removed from TRD §4, and removed from QA-1's public-read-path list, `.agents/tester.md`'s PII-boundary instruction, and the four `docs/ux-ui/design.md` places that called it "unbuilt" (it is now *cancelled*, which is a different status). `GET /api/v1/crops` row also removed — the three MVP crops stay a frontend constant by decision. **QA-1 also lost `/actors/geo`**, which does not exist either; if the still-open ATP-68 map decision creates it, QA-1 **and** QA-6 must both get it back. ⚠️ **QA-6 and TRD §4/§5/§13's remaining `/actors/geo` references are deliberately untouched** — they depend on the open map decision and are the rest of ATP-68. | docs/prd.md · docs/trd/trd.md · docs/ux-ui/design.md · .agents/tester.md · backend/src/test/pii-boundary.spec.ts (comment only) | Documentation + one JSDoc comment; no behaviour touched. `npx jest src/test/pii-boundary.spec.ts` 40 pass. No live reference to `/api/v1/export` or `/api/v1/crops` remains outside `docs/specs/archive/` and the two narrative log files. | [SPEC:quick/atp-68-scope-decisions] |
